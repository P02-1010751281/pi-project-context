# 摘要标题本地化：真机实跑证据（RPC，2026-09-16）

驱动 /tmp/handoff-headings-e2e.mjs：两轮中文小对话 → 两轮完整读取英文大文件 → `/auto-handoff now`（force）。
目的：让被摘要的一侧包含英文内容（复现「模型照抄 pi 模板标题」的冲突场景）。

## 子会话注入提示词（前 22 行）
```text
本会话接手上一会话（其上下文窗口已用 3.0%）。
下面的交接摘要覆盖上一会话较早的部分；其最近的消息已原文带入本会话。
动手前先用工具核对文件当前状态，不要重做已完成的工作。

## 交接摘要

## 目标
- 执行本地化验证：确认“验证摘要标题”的本地化，并记录两个检查点：
  1. 本次验证摘要标题的本地化。
  2. 注入提示词里的摘要标题必须是中文。
- 完整读取 `big1.md`（不传 `limit` 参数），并用一句话中文概括内容；不写文件、不提问。

## 约束与偏好
- 用户要求用一句话中文确认，不要提问。
- 读取 `big1.md` 时必须使用 `read` 工具完整读取，不要传 `limit` 参数。
- 不要写文件。
- 最终输出应为一句话中文概括。

## 进度
### 已完成
- [x] 已记录第一条检查点：本次验证摘要标题的本地化。
- [x] 已记录第二条检查点：注入提示词里的摘要标题必须是中文。
```

## .agents/memory/HANDOFF.md 标题
```text
# pi 会话 01a0a875-6398-71a8-b083-48433bd6d9e3 的交接文档
## 目标
## 约束与偏好
## 进度
### 已完成
### 进行中
### 阻塞
## 关键决策
## 下一步
## 关键上下文
```
